# Dashboard third-course reversal design

**Status:** Post-change QA PASS; delivery complete (unstaged/uncommitted)
**Date:** 2026-08-01
**Scope:** Remove Lantern Coil and restore the dashboard to two courses
**Delivery mode:** Gated Delivery

## Decision and objective

The user rejected Lantern Coil and explicitly directed that the dashboard return
to two courses. Remove the course completely from canonical source, compiler
configuration, catalog, SVG/CSS, generated artifacts, tests, screenshots, and
active roadmap status. Preserve Ridge Pass, Cypress Run, all item 1–5 behavior,
and the item 5 live workflow exactly.

The result must be functionally and byte-for-byte equivalent to the dashboard
state at `e61c3317ec52047e20b6d790277ab7989f3e2a04` for every item-6-touched
dashboard file, except for new durable reversal/future-work documentation.
Generated geometry and motion must be regenerated by the canonical compiler;
they may not be copied or hand-edited merely to match the parent commit.

Repository state at architecture freeze was `main` at
`47e087b6aa02b80cc3ffa2f60fcf0bf5b3058c7e`, equal to `origin/main` with
divergence `0/0`. The index is empty. The user-owned unstaged edit at
`wezterm/modules/background_manifests/anime.lua` is unrelated and excluded
from every operation. Current unrelated excluded root paths are `README.md`,
`scripts/background-bundles.sh`, and
`wezterm/modules/background_manifests/anime.lua`. The current `HEAD` and
`origin/main` remain `47e087b6aa02b80cc3ffa2f60fcf0bf5b3058c7e` with divergence
`0 behind / 0 ahead`; the index is empty. Reversal, roadmap, future-work, and
future-task-prompt changes remain unstaged or untracked. The user's follow-up
did not authorize staging, commit, or push.

## Approved implementation

1. Remove `lantern-coil` from `dashboard/routes/route-config.mjs`, restore the
   compiler fixed-route closure and runtime catalog cardinality to exactly two,
   and delete `dashboard/routes/lantern-coil.route.mjs`.
2. Regenerate `dashboard/src/generated/route-geometry.mjs` and
   `dashboard/generated/route-motion.css` through `routes:write`. Their final
   bytes must match `e61c331`.
3. Remove Lantern's selector option, SVG art, CSS, tests, browser coverage,
   documentation, and its two PNG references. All remaining item-6-touched
   dashboard files must match `e61c331` exactly.
4. Delete the rejected Lantern design packet
   `docs/superpowers/specs/2026-07-31-dashboard-third-course-design.md`.
5. Update `docs/superpowers/plans/2026-07-27-dashboard-roadmap.md`: items 1–5
   remain Complete; item 6 becomes Reverted, unimplemented, and unauthorized.
   It must not claim a third course is current.
6. Add `docs/superpowers/plans/2026-08-01-dashboard-future-work.md` containing
   only clearly unimplemented and unauthorized candidates:
   - design a replacement third original course;
   - strengthen neutral screenshot-state assertions;
   - add permanent breakpoint focus-clearance and pairwise phased-overlap
     assertions.
7. Add a self-contained start prompt at
   `docs/superpowers/prompts/2026-08-01-dashboard-breakpoint-regression-hardening.md`
   for the third candidate. The prompt must scope the work to test hardening,
   preserve the two-course runtime and screenshots, require synthetic inputs,
   prohibit real tmux access, define Git safety and verification, and state
   that executing the prompt is separate authorization.

The exact 21-path allowlist is:

```text
dashboard/README.md
dashboard/generated/route-motion.css
dashboard/index.html
dashboard/routes/lantern-coil.route.mjs                       (delete)
dashboard/routes/route-config.mjs
dashboard/scripts/lib/route-compiler.mjs
dashboard/src/generated/route-geometry.mjs
dashboard/src/track-catalog.mjs
dashboard/styles.css
dashboard/tests/BROWSER_VERIFICATION.md
dashboard/tests/browser/dashboard.spec.mjs
dashboard/tests/dashboard.test.mjs
dashboard/tests/multi-track.test.mjs
dashboard/tests/route-compiler.test.mjs
dashboard/tests/screenshots/desktop-lantern-coil.png           (delete)
dashboard/tests/screenshots/mobile-lantern-coil.png            (delete)
docs/superpowers/plans/2026-07-27-dashboard-roadmap.md
docs/superpowers/specs/2026-07-31-dashboard-third-course-design.md (delete)
docs/superpowers/specs/2026-08-01-dashboard-third-course-reversal-design.md
docs/superpowers/plans/2026-08-01-dashboard-future-work.md
docs/superpowers/prompts/2026-08-01-dashboard-breakpoint-regression-hardening.md
```

No other file is approved. Do not use `git revert`, checkout, restore, reset,
stash, or a broad staging command. Make explicit file edits and deletions while
preserving the unrelated WezTerm edit.

## Protected invariants

- Exactly two courses in order: Ridge Pass, Cypress Run.
- Schema v1, six segments, capacities `2/3/3/3/3/2`, responsive compiler
  profiles, 513 base samples plus boundaries, 64-second linear traversal,
  tangent headings, corner-aware drift, reduced motion, allocation, focus,
  hover, pin, tooltip, accessibility, and Cypress mobile scaling.
- Fixture and synthetic-live behavior, item 5 export/import/collector/schema/
  privacy/filesystem/refresh contracts, package manifests, and all unrelated
  integration files.
- No runtime dependency, backend, network, polling, telemetry, persistence,
  terminal integration, build system, or item 7.
- Automated tests must not access the real/default tmux server.

## Acceptance and verification

- Catalog/config/source directory/selector contain exactly Ridge and Cypress.
  No Lantern identifier, runtime data, UI, art, CSS, compiler output, test, or
  screenshot remains under `dashboard/**`. Historical text is permitted only
  in this retained reversal decision record and the roadmap/future-work list's
  explicit rejected, reverted, unimplemented, or unauthorized status. The
  rejected 2026-07-31 implementation design is absent.
- Every item-6-touched dashboard path that existed at `e61c331` is byte-identical
  to that commit; the four rejected item-6-only files are absent.
- `npm --prefix dashboard run routes:check`, unit tests, and the restored
  two-course browser baseline pass with zero failures and zero skips/todos.
- Every dashboard JavaScript module passes `node --check`; scoped whitespace
  checks pass; generated artifacts match compiler output; ports 43917/43918 are
  clear; browser console/page errors are zero.
- The restored 32-case browser baseline at desktop `1440×900` and mobile
  `390×844` covers Ridge/Cypress fixture rendering and switching, normal and
  reduced/fallback motion, focus, hover, pin, overflow, geometry, drift, and
  responsive Cypress behavior. Unit coverage remains authoritative for item 5
  synthetic-live import/source lifecycle contracts. Do not claim that the
  restored browser suite directly exercises synthetic-live mode or the four
  breakpoint edges. Existing nine screenshots remain byte-identical and must
  not be refreshed. Permanent breakpoint focus/overlap coverage is explicitly
  deferred to the unauthorized future-work task.
- Package/item-5/protected paths are unchanged, no real tmux is contacted, and
  only approved files may be staged or committed after independent post-QA.

## Builder implementation and verification — PASS

The native Builder completed only the approved 21-path reversal. The canonical
source/config/compiler inputs were restored first, the rejected route source
was deleted, and `npm --prefix dashboard run routes:write` reported
`routes: wrote 2 artifacts`. The regenerated artifact blob IDs are
`e119109df408cfbb1118955953c1f169f75266c3` for route geometry and
`c536b701647b5f3350faf2cbe70bc61feb8066f6` for route motion; both exactly
match `e61c331`.

All thirteen item-6-touched dashboard files retained from the two-course
baseline are byte-identical to `e61c331`. The rejected route source, two
Lantern screenshots, and 2026-07-31 implementation design are absent. A
case-insensitive residue search found no Lantern identifier under
`dashboard/**`.

Verification passed:

- `routes:check` reported generated artifacts current;
- 156/156 unit tests passed with zero failures, skips, or todos;
- the restored desktop/mobile Playwright baseline passed 32/32 in 4.3 minutes
  with zero failures or skips and clean console/page-error guards;
- all 39 dashboard JavaScript modules passed `node --check`;
- the complete nine-screenshot directory is byte-identical to `e61c331`;
- tracked and untracked scoped whitespace checks produced no diagnostics;
- ports 43917 and 43918 were clear, the index remained empty, and generated
  Playwright reports were removed;
- package/item-5 paths and the unrelated unstaged `anime.lua` edit remained
  unchanged; automated verification did not access a real/default tmux server.

No runtime observability was added. The different independent post-change QA
review returned PASS with no blocking finding. Nothing has been staged,
committed, or pushed.

## Observability

Add none. This is a removal and restoration. Existing compiler diagnostics,
unit/browser assertions, fatal UI behavior, and screenshot references are
sufficient.

## Gates

A separate native lead architect (`gpt-5.6-sol`, high reasoning) returned FAIL
on the initial packet, then PASS after all five blocking findings were corrected;
there was no role, model, or effort exception. Builder implemented only the
approved file set. A different native lead architect subsequently returned the
required post-change PASS, so the coordinator has finalized this delivery
handoff. That gate does not change the user's authority boundary: the reversal
is delivery-complete but explicitly unstaged and uncommitted, with no commit or
push authorized by the follow-up.

The future-work list and self-contained future-task prompt were created. The
prompt explicitly names breakpoint
pairs `759/760` and `959/960`, both retained courses in fixed Ridge/Cypress
order, focus-ring exterior clearance, pairwise phased-target overlap,
synthetic-only fixture/live inputs, normal/reduced/fallback states, no
screenshot or runtime change, no real/default tmux access, full Git safeguards
and verification commands. It is a proposed, unimplemented, and unauthorized
handoff: executing it requires separate user authorization and is not
authorized by this delivery.
